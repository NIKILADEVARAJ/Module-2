# Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
```
a = int(input("Enter the first number: "))
b = int(input("Enter the second number: "))
f = lambda a, b: a + b
print("Sum is:", f(a, b))
```
## Output
![image](https://github.com/user-attachments/assets/129a0f36-59fc-453e-b743-ebb4206f0353)

## Result
The program successfully uses a lambda function to compute the sum of two user-provided integers and displays the result.
